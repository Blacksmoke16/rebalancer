import { MantineProvider } from '@mantine/core';
import '@mantine/core/styles.css';

function App() {
    return (
        <MantineProvider>
                <h1>Vite + React</h1>
        </MantineProvider>
    );
}

export default App;
